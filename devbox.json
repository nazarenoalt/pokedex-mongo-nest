{
  "$schema": "https://raw.githubusercontent.com/jetify-com/devbox/main/.schema/devbox.schema.json",
  "packages": [
    "mongodb@7.0",
    "mongodb-tools",
    "mongodb-compass@latest"
  ],
  "shell": {
    "init_hook": [
      "echo 'Configurando MongoDB...'",
      "mkdir -p ./mongo",
      "echo 'MongoDB configurado en puerto 21909'"
    ],
    "scripts": {
      "start-db": [
        "mongod --dbpath ./mongo --port 21909 --bind_ip_all"
      ],
      "stop-db": [
        "mongod --shutdown --dbpath ./mongo"
      ]
    }
  },
  "env": {
    "MONGODB_DATABASE": "nest-pokemon",
    "MONGODB_PORT":     "21909",
    "MONGO_DATA_DIR":   "./mongo"
  }
}
